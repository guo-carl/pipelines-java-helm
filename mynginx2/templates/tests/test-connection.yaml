apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mynginx2.fullname" . }}-test-connection"
  labels:
    {{- include "mynginx2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mynginx2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
